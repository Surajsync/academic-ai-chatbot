# Chatbot System Architecture

The chatbot system follows a layered architecture.

Frontend
HTML pages interact with FastAPI endpoints.

Backend
FastAPI handles API requests.

Service Layer
Contains business logic.

Database Layer
Handles PostgreSQL operations.

Security Layer
Handles authentication using JWT.